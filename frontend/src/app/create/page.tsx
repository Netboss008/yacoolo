import CreateStream from '@/components/CreateStream';

export default function CreateStreamPage() {
  return (
    <main className="min-h-screen bg-primary-900 py-12">
      <div className="container mx-auto px-4">
        <CreateStream />
      </div>
    </main>
  );
} 